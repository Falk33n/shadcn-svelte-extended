import AlertDescription, { type AlertDescriptionProps } from './alert-description.svelte';
import AlertTitle, { type AlertTitleProps } from './alert-title.svelte';
import Alert, { type AlertProps } from './alert.svelte';

export {
	Alert,
	AlertDescription,
	AlertTitle,
	AlertDescription as Description,
	Alert as Root,
	AlertTitle as Title,
	type AlertDescriptionProps,
	type AlertProps,
	type AlertTitleProps,
	type AlertDescriptionProps as DescriptionProps,
	type AlertProps as RootProps,
	type AlertTitleProps as TitleProps,
};
