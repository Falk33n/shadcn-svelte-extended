import Button, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from './button.svelte';

export {
	Button,
	buttonVariants,
	Button as Root,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	type ButtonProps as RootProps,
};
