import AvatarFallback, { type AvatarFallbackProps } from './avatar-fallback.svelte';
import AvatarImage, { type AvatarImageProps } from './avatar-image.svelte';
import Avatar, { type AvatarProps } from './avatar.svelte';

export {
	Avatar,
	AvatarFallback,
	AvatarImage,
	AvatarFallback as Fallback,
	AvatarImage as Image,
	Avatar as Root,
	type AvatarFallbackProps,
	type AvatarImageProps,
	type AvatarProps,
	type AvatarFallbackProps as FallbackProps,
	type AvatarImageProps as ImageProps,
	type AvatarProps as RootProps,
};
